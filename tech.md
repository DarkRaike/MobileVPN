# Техническое задание: Telegram Mini App для управления VPN-подписками

**Статус:** Draft  
**Версия:** 1.1
**Дата:** 2026-07-27  
**Команда:** один разработчик и тимлид  
**Основной стек:** SvelteKit, TypeScript, Svelte 5, Tailwind CSS, SQLite, Drizzle ORM, Docker  
**VPN-контур:** Marzban, Xray-core, VLESS

## 1. Назначение документа

Документ определяет требования к веб-приложению, запускаемому как Telegram Mini App, и его интеграции с Marzban на одном сервере.

Приложение должно:

- безопасно авторизовывать пользователя по данным Telegram;
- показывать и продавать VPN-тарифы;
- принимать и учитывать промокоды;
- создавать и продлевать VPN-доступ через Marzban;
- показывать активную подписку, QR-код и ссылку подключения;
- принимать обращения в поддержку;
- предоставлять администратору компактную панель управления.

Документ ориентирован на реализацию одним разработчиком под ревью тимлида. Процессы, роли и инфраструктура, не дающие практической пользы небольшой команде, в первую версию не включаются.

## 2. Термины

- **Mini App** — веб-приложение, открываемое внутри Telegram.
- **Telegram user ID** — числовой идентификатор пользователя Telegram из проверенного `initData`. В переменных окружения и коде используется имя `TELEGRAM_ADMIN_USER_ID`; не следует называть этот идентификатор `chat_id`, если речь идёт об идентификации пользователя.
- **Администратор** — пользователь, чей проверенный Telegram user ID совпадает с `TELEGRAM_ADMIN_USER_ID`.
- **Тариф** — доступное для покупки предложение с названием, сроком и ценой.
- **Заказ** — намерение пользователя оплатить выбранный тариф.
- **Платёж** — запись о платёжной транзакции и её подтверждённом статусе.
- **Подписка приложения** — локальная запись о VPN-доступе пользователя.
- **Marzban user** — учётная запись VPN-пользователя в Marzban.
- **Subscription URL** — ссылка Marzban для получения конфигураций подключения.
- **VLESS** — основной протокол подключения, обслуживаемый Xray-core через Marzban.

## 3. Цели первой версии

1. Пользователь открывает Mini App из Telegram и автоматически проходит серверную аутентификацию.
2. Пользователь выбирает тариф, при необходимости применяет промокод и оплачивает заказ.
3. После подтверждения оплаты приложение создаёт или продлевает доступ в Marzban.
4. Пользователь получает ссылку подключения и QR-код.
5. Администратор управляет тарифами, промокодами и FAQ, просматривает обращения и основные операции.
6. Приложение и Marzban развёрнуты на одном сервере через Docker Compose с изолированными внутренними сервисами и HTTPS на публичных точках входа.

## 4. Вне объёма первой версии

- нативные приложения для iOS, Android, Windows или macOS;
- собственная реализация VPN-протокола;
- мультирегиональная инфраструктура и Marzban Node;
- несколько ролей администратора и сложная RBAC-модель;
- реферальная программа;
- автопродление с сохранением банковской карты;
- полноценная helpdesk-система с перепиской внутри Mini App;
- отдельная бухгалтерская система сверх обязательного flow чеков выбранного платёжного провайдера;
- отдельное мобильное или desktop-приложение администратора;
- Kubernetes, микросервисы, очереди сообщений и другие инфраструктурные усложнения без подтверждённой нагрузки.

## 5. Пользователи и права доступа

### 5.1. Пользователь

Может:

- просматривать активные тарифы;
- создавать заказ и переходить к оплате;
- применять доступный промокод;
- просматривать собственный профиль;
- просматривать собственную историю покупок;
- просматривать только собственную активную подписку;
- копировать Subscription URL и показывать QR-код;
- читать FAQ;
- отправлять обращение в поддержку.

Не может:

- получать данные других пользователей;
- менять цену, скидку, срок или итоговую сумму с клиента;
- вызывать административные операции;
- напрямую обращаться к Marzban API.

### 5.2. Администратор

Администратор имеет все права пользователя и доступ к административному разделу.

Права администратора:

- CRUD тарифов;
- CRUD промокодов;
- CRUD FAQ;
- просмотр и изменение статуса обращений;
- просмотр заказов, платежей и состояния выдачи подписок;
- повтор безопасной синхронизации заказа с Marzban после ошибки;
- деактивация тарифа или промокода без удаления исторических связей;
- просмотр минимального журнала административных действий.

Право администратора проверяется на сервере для каждого административного запроса. Скрытие кнопки в интерфейсе не является контролем доступа.

## 6. Основные пользовательские сценарии

### 6.1. Первый вход

1. Пользователь открывает Mini App из Telegram.
2. Клиент передаёт исходную строку `Telegram.WebApp.initData` серверу.
3. Сервер проверяет подпись, `auth_date` и обязательные поля.
4. Сервер создаёт либо обновляет локальный профиль пользователя.
5. Сервер создаёт защищённую сессию.
6. Пользователь попадает на раздел «Главная».

Если проверка Telegram не пройдена, доступ к пользовательским данным и бизнес-операциям запрещается. Интерфейс показывает понятную ошибку и предлагает повторно открыть приложение из Telegram.

### 6.2. Покупка

1. Пользователь выбирает активный тариф.
2. Сервер загружает актуальную цену и срок из базы данных.
3. Пользователь при необходимости применяет промокод.
4. Сервер повторно проверяет промокод и рассчитывает итоговую сумму.
5. Сервер создаёт заказ и одноразовый invoice link Telegram Stars.
6. Пользователь открывает invoice через `Telegram.WebApp.openInvoice()` и оплачивает его Stars.
7. Сервер проверяет `pre_checkout_query` и отвечает через Bot API не позднее 10 секунд.
8. После проверенного серверного Update с `message.successful_payment` платёж переводится в состояние `succeeded` один раз, идемпотентно.
9. Фоновая серверная операция создаёт или продлевает пользователя Marzban.
10. Локальная подписка синхронизируется с Marzban.
11. В профиле становятся доступны Subscription URL и QR-код.

Callback `openInvoice`, включая клиентский статус `paid`, и успешный ответ на `pre_checkout_query` не подтверждают оплату. Единственным обычным основанием выдачи доступа является проверенный сервером Telegram Update с `message.successful_payment`.

### 6.3. Продление

- Последовательные покупки разрешены, но суммарный продаваемый горизонт не может превышать 365 дней от момента оплаты.
- Новая дата рассчитывается как `max(actualMarzbanExpire, localExpire, paidAt) + durationDays`.
- Операция продления должна быть идемпотентной по идентификатору заказа.
- Повтор webhook или повторная синхронизация не должны добавлять срок второй раз.
- Уже оплаченный срок другого заказа нельзя сократить reconciliation или refund одного платежа.

### 6.4. Обращение в поддержку

1. Пользователь открывает «Поддержку».
2. Пользователь заполняет тему и описание проблемы.
3. Сервер валидирует и сохраняет обращение.
4. Сервер отправляет сообщение администратору через Telegram Bot API.
5. Пользователь получает номер обращения и подтверждение.

Ошибка отправки сообщения в Telegram не должна удалять уже сохранённое обращение. Запись получает статус доставки `failed`, ошибка журналируется, администратор может увидеть обращение в панели.

## 7. Структура и навигация Mini App

Приложение состоит из трёх основных секций:

1. **Поддержка** — слева.
2. **Главная** — по центру, открывается по умолчанию.
3. **Профиль** — справа.

### 7.1. Навигационный островок

В нижней безопасной области экрана размещается фиксированный навигационный островок в визуальном стиле Apple Liquid Glass:

- полупрозрачный фон;
- умеренное размытие содержимого под островком;
- тонкая светлая граница и мягкая тень;
- явно выделенный активный пункт;
- плавное, но короткое перемещение активного индикатора;
- иконка и доступная текстовая подпись для каждого пункта;
- адаптация цветов к Telegram theme params и системной светлой/тёмной теме.

Эффекты не должны ухудшать читаемость или производительность. Для устройств без поддержки `backdrop-filter` используется непрозрачный запасной фон.

Навигация поддерживает:

- нажатие на кнопки островка;
- горизонтальный свайп между соседними секциями;
- сохранение предсказуемого браузерного состояния;
- отключение или сильное сокращение анимаций при `prefers-reduced-motion`;
- отсутствие конфликта свайпа с вертикальной прокруткой и интерактивными элементами;
- минимальную область нажатия 44×44 CSS px.

Для первой версии допускается одна оболочка с тремя состояниями без трёх независимых URL, если корректно работает кнопка Back Telegram. Если потребуются прямые ссылки на секции, использовать маршруты `/`, `/support`, `/profile`.

### 7.2. Состояния интерфейса

Каждый экран с сетевыми данными должен иметь:

- загрузку;
- пустое состояние;
- ошибку с возможностью повтора;
- успешное состояние;
- блокировку повторной отправки во время мутации.

## 8. Функциональные требования

### 8.1. Главная

Главная показывает только активные тарифы в порядке, заданном администратором.

Начальный набор:

- 7 дней — 99 Stars;
- 30 дней — 249 Stars;
- 90 дней — 599 Stars.

Для всех трёх тарифов трафик безлимитный (`data_limit=0`). До трёх личных устройств — soft policy, а не обещание технического hard limit одновременных подключений.

Карточка тарифа содержит:

- название;
- краткое описание, если задано;
- срок в днях;
- исходную цену;
- валюту;
- итоговую цену после применённого промокода;
- размер скидки, если применимо;
- кнопку покупки;
- визуальную отметку рекомендуемого тарифа, если задана.

Цена, валюта, срок и идентификатор тарифа не считаются доверенными, если пришли от клиента. Перед созданием заказа сервер получает тариф из базы заново.

### 8.2. Поддержка

Раздел содержит:

- список опубликованных FAQ;
- раскрывающиеся ответы;
- форму обращения;
- результат отправки с номером обращения.

Поля обращения:

- тема: 3–120 символов;
- сообщение: 10–4000 символов.

Сообщение администратору содержит:

- номер обращения;
- Telegram user ID;
- username, если доступен;
- имя пользователя;
- тему;
- текст;
- дату и время UTC.

Текст пользователя экранируется в соответствии с выбранным `parse_mode` Telegram, чтобы исключить поломку форматирования и нежелательные ссылки/упоминания.

### 8.3. Профиль

Профиль показывает доступные из проверенного Telegram `initData` данные:

- avatar/photo URL, если Telegram его передал;
- имя;
- фамилию, если доступна;
- username, если доступен;
- Telegram user ID — только при необходимости диагностики.

Отсутствующие username или avatar отображаются корректно: для аватара используется инициала/placeholder, username не выдумывается.

#### 8.3.1. Промокод

Профиль содержит поле ввода промокода.

Требования:

- нормализация регистра и пробелов выполняется на сервере;
- валидность проверяется на сервере;
- отображается тип и размер скидки без раскрытия внутренних ограничений;
- применение промокода не резервирует скидку навсегда;
- промокод повторно проверяется атомарно при создании и подтверждении заказа;
- итоговая сумма не может быть отрицательной;
- использование учитывается только для успешно оплаченного заказа.

Выбранный промокод можно хранить только в `sessionStorage`; сервер всегда проверяет его заново. Пустой allowlist тарифов означает применимость ко всем активным тарифам.

#### 8.3.2. История покупок

Для каждой покупки отображаются:

- дата;
- тариф на момент покупки;
- исходная сумма;
- скидка;
- итоговая сумма;
- валюта;
- статус оплаты;
- статус выдачи VPN-доступа.

Исторические значения хранятся как snapshot в заказе и не меняются после редактирования тарифа.

#### 8.3.3. Активная подписка

Если подписка активна, отображаются:

- статус;
- дата начала;
- дата окончания;
- оставшееся время;
- Subscription URL;
- кнопка копирования;
- QR-код, содержащий тот же Subscription URL;
- краткая инструкция по подключению.

Если доступ оплачен, но Marzban временно недоступен, отображается состояние «Оплата получена, доступ создаётся» без предложения оплатить повторно.

Subscription URL считается секретом доступа:

- не записывается полностью в application logs;
- не передаётся сторонним аналитическим системам;
- не включается в URL страницы Mini App;
- показывается только владельцу после серверной авторизации.

### 8.4. Административный раздел

Ссылка на административный раздел видна в профиле только администратору. Серверные маршруты дополнительно защищены проверкой Telegram user ID.

#### 8.4.1. Тарифы

Поля:

- `name`;
- `description`;
- `durationDays`, целое число больше 0;
- `priceStars`, целое положительное количество Stars;
- `currency`, код из поддерживаемого списка;
- `isActive`;
- `isFeatured`;
- `sortOrder`.

Операции:

- создание;
- просмотр;
- редактирование;
- деактивация;
- удаление только тарифа, который нигде не используется.

Если тариф связан с заказами, вместо физического удаления используется деактивация.

#### 8.4.2. Промокоды

Поля:

- уникальный `code`;
- тип скидки: `percent` или `fixed`;
- значение скидки;
- валюта для фиксированной скидки;
- дата начала;
- дата окончания;
- общий лимит использований;
- лимит на одного пользователя;
- опциональный список разрешённых тарифов;
- `isActive`.

Правила:

- процент: больше 0 и не больше 100;
- фиксированная скидка применяется только к совместимой валюте;
- лимиты проверяются внутри транзакции;
- удаление использованного промокода запрещено, доступна деактивация.

#### 8.4.3. FAQ

Поля:

- вопрос;
- ответ;
- порядок;
- признак публикации.

#### 8.4.4. Обращения

Администратор может:

- просматривать обращения;
- фильтровать по статусу;
- переводить между `new`, `in_progress`, `resolved`;
- видеть статус доставки Telegram-уведомления.

#### 8.4.5. Заказы и выдача доступа

Администратор видит:

- идентификатор заказа;
- пользователя;
- платёжный статус;
- внешний ID платежа;
- статус provisioning;
- последнюю безопасную ошибку;
- число попыток;
- время следующего повтора.

Кнопка повтора доступна только для оплаченного заказа в ошибочном состоянии. Операция остаётся идемпотентной.

## 9. Интеграция с Telegram

### 9.1. Аутентификация Mini App

Сервер обязан:

1. Получить исходную строку `initData`, а не доверять `initDataUnsafe`.
2. Проверить HMAC-подпись по алгоритму Telegram.
3. Проверить допустимый возраст `auth_date`.
4. Выполнить constant-time comparison подписи.
5. Провалидировать схему вложенного объекта `user`.
6. Использовать Telegram user ID как строку или 64-битное целое без потери точности.
7. После проверки создать серверную сессию в cookie.

Рекомендуемый максимальный возраст `initData` для создания сессии — 5 минут. Значение делается конфигурируемым.

Cookie сессии:

- `HttpOnly`;
- `Secure` в production;
- `SameSite=Lax`;
- ограниченный срок жизни;
- ротация после повторной аутентификации;
- случайный непрогнозируемый идентификатор;
- серверное отзывное состояние.

Разработка вне Telegram допускается только через явно включаемый development-only mock. В production mock-auth должен завершать запуск с ошибкой конфигурации.

### 9.2. Telegram Bot API

Bot token хранится только в серверной переменной окружения.

Бот используется для:

- отправки обращений администратору;
- опциональных уведомлений пользователю о готовности подписки;
- опциональных уведомлений администратору об ошибках provisioning.

В первой версии используется один bot token для Mini App, обращений и уведомлений. Администратор отвечает пользователю через этот же бот. После успешной выдачи или продления бот отправляет короткое уведомление без Subscription URL.

## 10. Интеграция с Marzban

### 10.1. Общие требования

- Взаимодействие выполняется только серверным адаптером.
- Marzban API не публикуется в интернет без необходимости.
- Приложение обращается к Marzban по внутренней Docker-сети.
- Учётные данные Marzban не попадают в браузер, базу заказов или логи.
- Используется VLESS inbound, согласованный с эксплуатационной конфигурацией.
- Таймаут, ограниченное число повторов и backoff обязательны.
- Ответы Marzban проходят runtime-валидацию.

### 10.2. Идентификатор пользователя Marzban

Рекомендуемый формат: непрозрачный стабильный идентификатор, производный от локального UUID пользователя, например `tg_<short-uuid>`. Не использовать username Telegram: он необязателен и может измениться.

Локальная база хранит:

- `marzbanUsername`;
- `marzbanStatus`;
- `subscriptionUrl` в защищённом виде либо минимальные данные для безопасного получения;
- `expiresAt`;
- дату последней синхронизации;
- последнюю нормализованную ошибку.

### 10.3. Создание и продление

После подтверждения оплаты provisioning worker:

1. Блокирует конкретный заказ на обработку.
2. Проверяет, не был ли заказ уже применён.
3. Получает текущее состояние пользователя Marzban.
4. Рассчитывает ожидаемую дату окончания по серверным данным.
5. Создаёт или обновляет Marzban user.
6. Получает Subscription URL.
7. В одной локальной транзакции обновляет подписку и помечает заказ применённым.

Если внешний вызов успешен, а локальная транзакция не завершилась, повтор сначала сверяет фактическое состояние Marzban и не продлевает подписку вслепую.

### 10.4. QR-код

QR-код генерируется приложением из Subscription URL после серверной авторизации. Предпочтительно формировать SVG на сервере или локально в браузере без передачи ссылки внешнему QR-сервису.

### 10.5. Внешний контракт Marzban

Зафиксирован Marzban `v0.8.4`, commit `7f396db3e703d71a28060bc9ce4a532ec64cb1f4` и OCI image index:

```text
gozargah/marzban:v0.8.4@sha256:8e422c21997e5d2e3fa231eeff73c0a19193c20fc02fa4958e9368abb9623b8d
```

OpenAPI snapshot хранится в `contracts/marzban/openapi.v0.8.4.json`; его SHA-256 — `1f38142daa8a4b636ed11b16e9511583df81cc5373db964f88fd94206f1608dc`.

Адаптер использует только:

- `POST /api/admin/token` — OAuth2 password form, получение Bearer token;
- `GET /api/user/{username}` — чтение фактического состояния перед повтором;
- `POST /api/user` — создание;
- `PUT /api/user/{username}` — изменение и продление.

API доступен только во внутренней Docker-сети. Runtime-схемы строятся по зафиксированным `Token`, `UserCreate`, `UserModify` и `UserResponse`. Production docs/OpenAPI отключены.

Выбранная версия требует отдельного SBOM/CVE review образа и bundled Xray перед production из-за возраста релиза. Обновление версии выполняется только вместе с новым snapshot, hash и контрактными тестами.

### 10.6. VLESS/REALITY

Первая версия использует один inbound:

- tag `VLESS TCP REALITY`;
- VLESS + RAW/TCP на 8443;
- REALITY;
- flow отсутствует — обычный VLESS;
- `decryption=none`;
- `data_limit=0`;
- `data_limit_reset_strategy=no_reset`.

Отказ от `xtls-rprx-vision` — измеренное решение, заменяющее исходное требование этого раздела. Vision выполняет splicing трафика после заголовка VLESS, и его формат расходится между закреплённым Xray `24.12.31` и ядрами, на которых собраны актуальные мобильные клиенты: REALITY проходит аутентификацию, передаётся несколько сотен байт рукопожатия, после чего туннель не несёт ничего. Отказ виден только по счётчику трафика и не логируется. Возврат Vision допустим вместе с обновлением ядра и повторным измерением на реальном клиенте.

Caddy использует 80/443, поэтому VLESS вынесен на 8443 без port multiplex и custom Caddy modules. Если целевая сеть блокирует 8443, изменение на stream proxy или отдельный IP оформляется отдельным измеренным решением.

REALITY target и `serverNames` выбираются по измерениям с production VPS через `xray tls ping`; target должен поддерживать TLS 1.3, а имя — соответствовать SAN. X25519 private key и случайный 16-символьный hex `shortId` хранятся только в secret storage. Синхронизация времени обязательна. До фиксации target и keys production-запуск запрещён.

## 11. Платежи через Telegram Stars

### 11.1. Выбранная интеграция

В первой версии используется только **Telegram Stars**. Для цифровых товаров и услуг внутри Telegram это обязательный способ оплаты. Валюта Bot API — `XTR`, сумма — целое количество Stars. Рекуррентные платежи не используются.

Начальные цены: 7 дней — 99 Stars, 30 дней — 249 Stars, 90 дней — 599 Stars. Это самостоятельные продуктовые цены без runtime-конвертации из рублей.

Внешний эквайринг и оплата вне Telegram не входят в первую версию. Их нельзя добавлять через недоделанную универсальную абстракцию: будущий провайдер требует отдельного решения, юридической проверки и контрактных тестов.

### 11.2. Создание invoice link

1. Сервер повторно загружает тариф, проверяет промокод и рассчитывает итоговое целое количество Stars.
2. Сервер создаёт локальный заказ и payment attempt со статусом `pending`.
3. `invoice_payload` имеет формат `v1:<payment-attempt-uuid>`, содержит только непрозрачный идентификатор и занимает не более 128 байт.
4. Сервер вызывает Bot API `createInvoiceLink` с `currency=XTR`, пустым `provider_token` и ровно одним `LabeledPrice`.
5. Не запрашиваются имя, телефон, email, адрес, доставка и tips.
6. Клиент получает invoice URL и открывает его через `Telegram.WebApp.openInvoice()` только по прямому пользовательскому действию.

Callback `openInvoice` со статусами `paid`, `cancelled`, `failed` или `pending` используется только для UI. Даже `paid` на клиенте не является основанием выдачи доступа.

Нельзя включать в title, description или payload:

- Telegram `initData`;
- bot token и webhook secret;
- Subscription URL;
- данные доступа Marzban;
- персональные данные;
- текст обращения в поддержку.

### 11.3. Telegram webhook и pre-checkout

Endpoint: `POST /api/telegram/webhook`.

Webhook настраивается через `setWebhook` с `secret_token`. Каждый запрос проверяет заголовок `X-Telegram-Bot-Api-Secret-Token` constant-time сравнением с `TELEGRAM_WEBHOOK_SECRET`. Тело ограничивается 64 KiB, `update_id` дедуплицируется, разрешённые типы включают минимум `message` и `pre_checkout_query`.

На `pre_checkout_query` Bot API требует ответить не позднее 10 секунд. Внутренний deadline — 8 секунд. До `answerPreCheckoutQuery(ok=true)` сервер сверяет:

- `from.id` с Telegram user ID владельца заказа;
- `invoice_payload` с открытым payment attempt;
- `currency=XTR`;
- `total_amount` со snapshot заказа;
- что заказ не оплачен, не отменён и ещё может быть выдан;
- максимальный горизонт подписки 365 дней.

При расхождении отправляется `ok=false` с безопасным пользовательским сообщением. SQLite-транзакция не удерживается во время вызова Bot API. Успешный ответ на pre-checkout разрешает продолжить оплату, но не подтверждает её.

### 11.4. Подтверждение и reconciliation

Единственный обычный источник подтверждения — серверный Update с `message.successful_payment`.

Перед переходом платежа в `succeeded` проверяются:

- webhook secret;
- `message.from.id`;
- `invoice_payload`;
- `currency=XTR`;
- `total_amount`;
- непустой `telegram_payment_charge_id`;
- отсутствие ранее обработанного `update_id` и charge ID;
- заказ ещё не применён к подписке.

Payment event, charge ID и переход заказа сохраняются транзакционно до запуска Marzban provisioning. Webhook получает `2xx` после надёжного сохранения; provisioning выполняется вне критического пути. Повтор Update не продлевает доступ повторно.

Для восстановления пропущенных событий используется `getStarTransactions`: сверяются transaction ID, `source.invoice_payload`, пользователь и amount. Telegram хранит непринятые updates не более 24 часов, поэтому reconciliation и мониторинг обязательны.

### 11.5. Серверный Telegram Stars-адаптер

Bot token доступен только в `$lib/server`. Нужен небольшой конкретный адаптер:

```ts
interface TelegramStarsPayments {
  createInvoiceLink(input: CreateStarsInvoiceInput): Promise<string>;
  answerPreCheckout(input: AnswerPreCheckoutInput): Promise<void>;
  refundPayment(input: RefundStarsPaymentInput): Promise<void>;
  getTransactions(input: GetStarTransactionsInput): Promise<StarTransactionPage>;
}
```

Полный возврат выполняется серверным `refundStarPayment` по Telegram user ID и `telegram_payment_charge_id`. Операция имеет состояния `refund_requested`, `refund_pending`, `refunded`/`refund_failed`, сохраняет безопасное evidence и идемпотентна по payment ID. Частичный возврат не поддерживается. После подтверждённого возврата отзывается только срок, выданный этим заказом.

Бот обязан предоставлять `/terms` и `/paysupport`; пользователь подтверждает условия до создания invoice. Владелец бота отвечает за споры и своевременную поддержку. Юридический и налоговый режим Stars подтверждается профильным специалистом до production.

### 11.6. Состояния

Заказ:

- `pending_payment`;
- `paid`;
- `provisioning`;
- `active`;
- `provisioning_failed`;
- `cancelled`;
- `refunded`.

Платёж:

- `pending`;
- `succeeded`;
- `failed`;
- `cancelled`;
- `refunded`.

Переходы выполняются только разрешёнными серверными командами. Повтор webhook, successful payment или reconciliation возвращает успешный результат без повторного применения.

### 11.7. Денежные значения

- Хранить только целое количество Stars.
- Валюта каждого заказа и платежа — `XTR`.
- Не конвертировать RUB, USD или стоимость покупки Stars в цену тарифа во время запроса.
- Заказ хранит snapshot названия тарифа, срока, цены и скидки.
- Итог пересчитывается сервером.
- Значения `SuccessfulPayment.total_amount`, `currency`, `invoice_payload`, Telegram user ID и charge ID сверяются с локальным заказом перед provisioning.

## 12. Архитектура приложения

### 12.1. Подход

Рекомендуется модульный монолит SvelteKit. Для одного разработчика это даёт минимальную операционную сложность, сохраняя разделение ответственности.

Слои:

- **routes/UI** — страницы, layouts, form actions и HTTP endpoints;
- **application** — use cases: создание заказа, применение промокода, подтверждение платежа, provisioning;
- **domain** — правила тарифов, скидок, заказов и подписок;
- **infrastructure** — Drizzle repositories, Telegram Bot API, Marzban adapter, payment provider;
- **shared** — схемы валидации, типы ошибок и безопасные утилиты.

SvelteKit server-only код размещается под `$lib/server`.

Пример структуры:

```text
src/
  lib/
    components/
    features/
      navigation/
      plans/
      profile/
      support/
      admin/
    server/
      auth/
      db/
      modules/
        users/
        plans/
        promo-codes/
        orders/
        subscriptions/
        support/
      integrations/
        marzban/
        payments/
        telegram/
      jobs/
    validation/
  routes/
    (app)/
    admin/
    api/
      auth/telegram/
      payments/webhook/
      internal/jobs/
```

Конкретная структура может быть упрощена, если модуль состоит из одного небольшого файла. Пустые слои и интерфейсы «на будущее» не создаются.

### 12.2. OOP и DRY

- OOP используется там, где объект инкапсулирует состояние или внешний контракт: адаптеры, сервисы, state machine.
- Простые преобразования и правила реализуются чистыми функциями.
- Наследование не используется без необходимости; предпочтительны композиция и небольшие интерфейсы.
- DRY применяется к знаниям и бизнес-правилам, а не к случайно похожим строкам кода.
- Допустима небольшая локальная дубликация, если общая абстракция сделает код сложнее.
- Доменные правила не дублируются между клиентом и сервером: клиентская проверка улучшает UX, серверная остаётся обязательной.

### 12.3. Серверные операции

- Чтение страницы выполняется через server `load`.
- Формы приложения по умолчанию используют SvelteKit form actions с progressive enhancement.
- Отдельные `+server.ts` endpoints используются для webhook, Telegram auth и контрактов, не являющихся обычными HTML-формами.
- Все входные данные проходят runtime-валидацию.
- Ошибки внешних систем преобразуются в стабильные внутренние коды.

## 13. Модель данных

Все таблицы содержат `createdAt` и `updatedAt`, если не указано иное. Время хранится в UTC.

### 13.1. `users`

- `id` — UUID, PK;
- `telegramUserId` — text, unique, not null;
- `username` — text, nullable;
- `firstName` — text, not null;
- `lastName` — text, nullable;
- `photoUrl` — text, nullable;
- `languageCode` — text, nullable;
- `lastAuthenticatedAt` — integer/datetime, not null.

### 13.2. `sessions`

- `idHash` — text, PK; хранится hash, не исходный session token;
- `userId` — FK users;
- `expiresAt`;
- `lastSeenAt`;
- `revokedAt` — nullable.

### 13.3. `plans`

- `id` — UUID, PK;
- `name`;
- `description` — nullable;
- `durationDays`;
- `priceStars`;
- `currency`;
- `isActive`;
- `isFeatured`;
- `sortOrder`.

Ограничения: `durationDays > 0`, `priceStars > 0`, `currency = 'XTR'`, один рекомендуемый активный тариф — по решению продукта.

### 13.4. `promo_codes`

- `id` — UUID, PK;
- `codeNormalized` — unique;
- `discountType`;
- `discountValue`;
- `currency` — nullable;
- `startsAt` — nullable;
- `endsAt` — nullable;
- `maxUses` — nullable;
- `maxUsesPerUser` — nullable;
- `isActive`.

Связь с разрешёнными тарифами — `promo_code_plans`.

### 13.5. `orders`

- `id` — UUID, PK;
- `userId`;
- `planId`;
- `promoCodeId` — nullable;
- snapshot полей тарифа;
- `subtotalStars`;
- `discountStars`;
- `totalStars`;
- `currency`;
- `status`;
- `provisioningStatus`;
- `provisioningAttempts`;
- `provisioningErrorCode` — nullable;
- `provisionedAt` — nullable;
- `idempotencyKey` — unique.

### 13.6. `payments`

- `id` — UUID, PK;
- `orderId`;
- `provider` — константа `telegram_stars` в первой версии;
- `invoicePayload` — unique, `v1:<payment-attempt-uuid>`;
- `telegramPaymentChargeId` — nullable, unique;
- `status`;
- `amountStars`;
- `currency`;
- `paidAt` — nullable;
- `refundedAt` — nullable;
- `providerPayloadSafe` — nullable, только минимально необходимые несекретные данные.

### 13.7. `payment_events`

- `provider` — `telegram_stars`;
- `externalEventId` — строковое представление Telegram `update_id`;
- `eventType`;
- `receivedAt`;
- `processedAt` — nullable;
- `processingErrorCode` — nullable.

Уникальный индекс `(provider, externalEventId)`.

### 13.8. `refunds`

- `id` — UUID, PK;
- `paymentId`;
- `amountStars`;
- `currency`;
- `status`;
- `reasonCode`;
- `providerEvidenceSafe` — nullable;
- `requestedAt`;
- `confirmedAt` — nullable;
- `failedAt` — nullable.

Один подтверждённый полный refund на платёж защищается уникальным индексом. Provider evidence не содержит payer details, секреты или Subscription URL.

### 13.9. `subscriptions`

- `id` — UUID, PK;
- `userId` — unique;
- `marzbanUsername` — unique;
- `status`;
- `startsAt`;
- `expiresAt`;
- `subscriptionUrlEncrypted` — nullable;
- `lastSyncedAt`;
- `version` — optimistic locking.

### 13.10. `order_provisioning`

- `orderId` — unique;
- `appliedDurationDays`;
- `targetExpiresAt`;
- `state`;
- `lockedAt` — nullable;
- `nextAttemptAt` — nullable;
- `lastErrorCode` — nullable.

### 13.11. `faq_items`

- `id` — UUID, PK;
- `question`;
- `answer`;
- `sortOrder`;
- `isPublished`.

### 13.12. `support_tickets`

- `id` — UUID, PK;
- `publicNumber` — unique;
- `userId`;
- `subject`;
- `message`;
- `status`;
- `telegramDeliveryStatus`;
- `telegramMessageId` — nullable;
- `resolvedAt` — nullable.

### 13.13. `admin_audit_log`

- `id`;
- `adminUserId`;
- `action`;
- `entityType`;
- `entityId`;
- `beforeJson` — nullable, без секретов;
- `afterJson` — nullable, без секретов;
- `createdAt`.

## 14. SQLite и Drizzle ORM

- SQLite используется только для данных приложения. Marzban управляет собственной базой отдельно.
- Включить WAL mode, foreign keys и разумный `busy_timeout`.
- Все изменения схемы выполняются версионируемыми Drizzle migrations.
- Миграции запускаются отдельной одноразовой командой перед стартом новой версии приложения.
- Денежные значения и Telegram user ID не преобразуются в небезопасные JS-числа.
- Критические операции заказа, промокода и payment event выполняются в транзакциях.
- Для конкурентных webhook и provisioning используются уникальные ограничения, conditional update и короткие транзакции.
- Нельзя удерживать SQLite-транзакцию во время HTTP-запроса к Marzban или платёжному провайдеру.

SQLite подходит для первой версии и одного экземпляра приложения. Переход на PostgreSQL рассматривается, если подтверждены высокая конкуренция записей, несколько экземпляров приложения или требования высокой доступности.

## 15. Docker и размещение на одном сервере

### 15.1. Сервисы

Ожидаемый Docker Compose:

- `reverse-proxy` — Caddy;
- `app` — SvelteKit Node server;
- `worker` — тот же image с процессом обработки provisioning/retry либо безопасный периодический runner;
- `marzban`;
- компоненты Marzban/Xray согласно выбранному официальному варианту развёртывания.

Для первой версии `app` и `worker` могут быть одним процессом, если обработка восстанавливается после рестарта и не зависит от in-memory очереди.

### 15.2. Сети и порты

Доменные роли:

- `app.{baseDomain}` — Mini App, legal pages и `POST /api/telegram/webhook`;
- `sub.{baseDomain}` — только Marzban `/sub/*`;
- `vpn.{baseDomain}` — VLESS/REALITY.

Конкретный принадлежащий проекту `baseDomain` задаётся production-конфигурацией и обязателен для запуска. Публичного admin-домена нет.

Порты:

- 80/443 — Caddy и HTTPS;
- 8443/TCP — `VLESS TCP REALITY`;
- 22 — SSH только по operator IP allowlist;
- 3000, 8000 и 2019 — только приватная Docker-сеть.

На subscription host пути `/api/*`, `/dashboard/*`, `/docs` и `/openapi.json` отвечают `404`. Access log этого host отключён, чтобы subscription token не сохранялся. Marzban admin доступен только через SSH tunnel. SQLite-файлы приложения и данные Marzban находятся в разных volumes.

Caddy image фиксируется:

```text
caddy:2.11.4-alpine@sha256:5f5c8640aae01df9654968d946d8f1a56c497f1dd5c5cda4cf95ab7c14d58648
```

### 15.3. Ресурсы

- Контейнеры имеют restart policy и healthcheck.
- Image версии фиксируются, `latest` не используется.
- Контейнеры запускаются без root, если образ и сетевые требования это позволяют.
- Файловая система контейнера read-only, кроме явно подключённых каталогов.
- Секреты не встраиваются в image.

### 15.4. Резервное копирование

Ежечасно резервируются:

- SQLite приложения и Marzban через Online Backup API или `.backup`, а не копированием живого WAL-файла;
- migrations и конфигурация развертывания;
- данные Marzban согласно официальной документации;
- необходимые ключи и конфигурация Xray в зашифрованном виде.

Требования:

- restic repository у другого provider, account и region;
- шифрование backup, ключ хранится вне VPS;
- Object Lock governance минимум 7 дней, если поддерживается storage;
- retention: hourly 48 часов, daily 30 дней, weekly 12 недель, максимум 84 дня;
- `PRAGMA integrity_check`, `PRAGMA foreign_key_check` и SHA-256 manifest;
- alert при отсутствии успешной копии более 65 минут;
- test restore до production launch и затем минимум раз в квартал в чистом одноразовом окружении;
- RPO не более 60 минут, RTO не более 4 часов;
- restore поверх live volume запрещён.

Полная процедура находится в `docs/operations/backup-restore.md`.

## 16. Безопасность

### 16.1. Обязательные меры

- Проверка Telegram `initData` только на сервере.
- Server-side authorization для каждого ресурса.
- Runtime-валидация всех входных данных и внешних ответов.
- Параметризованные запросы через Drizzle.
- CSRF-защита для cookie-authenticated мутаций; использовать встроенные ограничения SvelteKit и проверять `Origin` для чувствительных endpoints.
- Проверка подписи и replay protection webhook.
- Rate limiting для auth, промокодов, поддержки, платежей и admin endpoints.
- Общие ошибки без утечки stack trace и секретов.
- Security headers: CSP, `X-Content-Type-Options`, `Referrer-Policy`, ограниченный `Permissions-Policy`, HSTS после корректной настройки HTTPS.
- Ограничение размера request body.
- Redaction токенов, cookie, webhook secrets, Subscription URL и персональных данных в логах.
- Зависимости проверяются на известные уязвимости перед релизом.
- Production source maps не публикуются без контроля доступа.

### 16.2. Секреты

Секретные значения:

- Telegram bot token;
- session secret/key;
- Marzban credentials;
- Telegram webhook secret;
- ключ шифрования Subscription URL;
- приватные ключи Xray/REALITY.

Требования:

- только server-side env/secret files;
- `.env` не коммитится;
- в репозитории хранится `.env.example` без значений;
- разные секреты для development и production;
- ротация документируется;
- доступ выдаётся по принципу минимальных привилегий.

### 16.3. Персональные данные

Хранить только данные, необходимые для работы сервиса. Операционные сроки:

- active session: 7 дней absolute и 24 часа idle;
- revoked/expired session: 30 дней;
- пользователь без покупок: удалить после 12 месяцев неактивности;
- direct Telegram identifiers пользователя с покупками: анонимизировать после 24 месяцев неактивности, если закон не требует дольше;
- Subscription URL: активная подписка плюс 30 дней;
- expired Marzban user: 30 дней;
- support body: 180 дней после закрытия;
- support delivery metadata и admin audit log: 1 год;
- redacted application/security logs: 30 дней;
- Xray error log: 7 дней, access log отключён;
- backup: не более 84 дней.

Ежедневная purge job идемпотентна, обрабатывает bounded batches и соблюдает legal hold. Срок хранения заказов, платежей, refund и чеков устанавливается утверждённым юридическим и бухгалтерским schedule до production.

До production профильный специалист подтверждает юрисдикцию, оферту, privacy/refund policy, фискализацию, обработку персональных данных и законность предоставления/рекламы VPN в целевых странах. Эти подтверждения являются fail-closed production gates, а не неявными предположениями реализации.

## 17. Переменные окружения

Минимальный предварительный список:

```dotenv
NODE_ENV=
ORIGIN=
DATABASE_URL=

TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
TELEGRAM_ADMIN_USER_ID=
TELEGRAM_INIT_DATA_MAX_AGE_SECONDS=300

SESSION_SECRET=
SUBSCRIPTION_URL_ENCRYPTION_KEY=

MARZBAN_BASE_URL=
MARZBAN_USERNAME=
MARZBAN_PASSWORD=
MARZBAN_VLESS_INBOUND_TAG=VLESS TCP REALITY

BASE_DOMAIN=

RESTIC_REPOSITORY=
RESTIC_PASSWORD_FILE=

LOG_LEVEL=info
```

`TELEGRAM_WEBHOOK_SECRET` генерируется отдельно от bot token, передаётся в `setWebhook.secret_token` и проверяется по заголовку каждого Update. Приложение валидирует env при старте и завершает production-запуск, если отсутствуют обязательные секреты или evidence gates.

## 18. Нефункциональные требования

### 18.1. Производительность

Целевые показатели первой версии при нормальной мобильной сети:

- серверный p95 для обычных read-запросов без внешних API — до 500 мс;
- p95 серверной мутации без внешних API — до 800 мс;
- первая полезная отрисовка — до 2,5 с на среднем мобильном устройстве;
- навигация и свайп — без заметных пропусков кадров;
- изображения оптимизированы, лишний JavaScript не загружается.

Provisioning и платежи не подчиняются этим значениям из-за внешних API, но UI сразу показывает промежуточное состояние.

### 18.2. Надёжность

- Повтор webhook безопасен.
- Повтор provisioning безопасен.
- Рестарт приложения не теряет оплаченные заказы.
- Временная недоступность Telegram или Marzban не приводит к потере локальных данных.
- Заказы в промежуточном состоянии автоматически находятся reconciliation job.
- Все timestamps хранятся в UTC, локализация выполняется при отображении.

### 18.3. Доступность

- Семантическая HTML-разметка.
- Полная навигация с клавиатуры.
- Видимый focus state.
- Достаточный контраст поверх glass-эффекта.
- ARIA используется только там, где семантики HTML недостаточно.
- Минимальная область интерактивных элементов 44×44 px.
- Поддержка `prefers-reduced-motion`.

### 18.4. Совместимость

Поддерживаются актуальные версии Telegram для iOS, Android и Desktop, доступные на момент релиза. Обязательны smoke-тесты минимум на:

- Telegram iOS;
- Telegram Android;
- Telegram Desktop;
- обычный браузер с диагностическим сообщением при запуске вне Telegram.

Минимальная требуемая Telegram WebApp API version — 6.1. Перед каждым production release в evidence record фиксируются фактически проверенные актуальные версии iOS, Android и Desktop; непроверенный клиентский набор блокирует release.

## 19. Логирование, мониторинг и аудит

Структурированные JSON-логи содержат:

- timestamp;
- level;
- request/correlation ID;
- route/use case;
- безопасный user ID или internal UUID;
- order ID;
- внешний сервис;
- error code;
- duration.

Не логируются:

- bot token и API keys;
- cookie/session token;
- исходный `initData`;
- полный webhook payload без фильтрации;
- полный Subscription URL;
- пароли и приватные ключи.

Минимальные метрики/сигналы:

- частота 5xx;
- ошибки Telegram auth;
- неподтверждённые платежи старше порога;
- оплаченные заказы без активной подписки;
- ошибки provisioning;
- недоступность Marzban;
- ошибки отправки обращений;
- состояние backup.

Для одного разработчика достаточно error tracking и внешнего uptime-check. Полный observability stack вводится только при подтверждённой необходимости.

## 20. Тестирование

### 20.1. Unit tests

Обязательно покрыть:

- проверку Telegram `initData`, включая неверную подпись и устаревший `auth_date`;
- расчёт процентной и фиксированной скидки;
- лимиты и сроки промокода;
- денежные округления;
- переходы state machine заказа и платежа;
- расчёт даты продления;
- идемпотентность повторного payment event;
- redaction секретов.

### 20.2. Integration tests

- Drizzle repositories на временной SQLite базе с migrations;
- транзакционное применение промокода;
- создание заказа;
- Telegram Stars pre-checkout, successful payment, refund и `getStarTransactions` reconciliation;
- provisioning с mock Marzban API;
- повтор provisioning после частичного сбоя;
- проверка admin authorization;
- сохранение обращения при недоступности Telegram.

### 20.3. End-to-end tests

Критический happy path:

1. авторизация;
2. выбор тарифа;
3. промокод;
4. подтверждённая тестовая оплата;
5. создание подписки;
6. отображение QR и ссылки.

Критические негативные сценарии:

- поддельный Telegram `initData`;
- клиент подменяет цену;
- повторный webhook и повторный status reconciliation;
- обычный пользователь открывает admin URL;
- Marzban недоступен после успешной оплаты;
- использованный или истёкший промокод;
- повторное нажатие кнопки оплаты.

### 20.4. Проверки перед merge

- format;
- lint;
- type check;
- unit/integration tests;
- production build;
- проверка migrations на чистой базе.

## 21. Топ-5 рекомендаций по SvelteKit

### 1. Использовать Svelte 5 runes для нового кода

- `$state` — только для изменяемого реактивного состояния;
- `$derived` — для вычисляемых значений;
- `$effect` — только для синхронизации с внешней системой или DOM, не для обычных вычислений;
- `$props` — для входных свойств компонентов;
- реактивную логику, которую нужно переиспользовать, выносить в `.svelte.ts`.

Это делает зависимости явными и уменьшает количество legacy stores и неочевидных `$:`-эффектов.

### 2. Держать доверенные операции на серверной стороне SvelteKit

Сессии, цены, скидки, admin checks, Marzban и платежи должны жить в `+page.server.ts`, `+server.ts`, server hooks и `$lib/server`. Компонент отвечает за интерфейс, а не за доверенную бизнес-логику.

### 3. Использовать server `load` для чтения и form actions для обычных мутаций

Form actions дают типизированный серверный POST, единый возврат ошибок и progressive enhancement. Самописный клиентский `fetch` использовать для webhook, нестандартного API или интерфейса, где form action действительно не подходит.

### 4. Использовать сгенерированные типы маршрутов и runtime-валидацию

Применять `PageProps`, `LayoutProps`, `RequestHandler`, `Actions` и типы из `./$types`. TypeScript не проверяет данные сети во время выполнения, поэтому границы Telegram, payment, Marzban, form data и env должны иметь runtime schema.

### 5. Минимизировать клиентское состояние и объём JavaScript

Данные страницы загружать через SvelteKit, состояние фильтров и выбранных представлений хранить в URL или локально рядом с компонентом. Не вводить глобальный store для данных, которые уже предоставляет серверный `load`. Lazy-load тяжёлые части админки и QR-библиотеку, не отключать SSR без измеримой причины.

## 22. Стандарты кода и Git

### 22.1. Обязательные инженерные правила

Единым источником обязательных требований к реализации является соседний файл [`AGENTS.md`](./AGENTS.md). Codex читает его перед началом работы. Файл определяет правила архитектуры, SvelteKit/Svelte, аутентификации, Telegram Stars, Marzban, SQLite, безопасности, тестирования, комментариев и Git.

Ключевые ограничения:

- TypeScript strict mode;
- доверенные операции и authorization только на сервере;
- Telegram Stars updates/reconciliation и Marzban provisioning должны быть идемпотентными;
- комментарии и Conventional Commits — краткие и на английском языке;
- автор коммитов — `DarkRaike <danikzhurik64@gmail.com>`;
- format, lint, type check, tests, build и migration checks обязательны перед merge;
- в коде, комментариях, коммитах и PR не оставляются служебные упоминания инструментов подготовки изменений.

Если `tech.md` и `AGENTS.md` расходятся, требования безопасности, продуктовые критерии и внешние контракты из `tech.md` имеют приоритет; для повседневных правил реализации используется `AGENTS.md`.

### 22.2. Рабочий процесс

Для одного разработчика и тимлида:

1. Небольшая задача оформляется с короткими критериями приёмки.
2. Разработчик создаёт короткоживущую ветку.
3. Перед PR выполняются автоматические проверки.
4. Тимлид проверяет безопасность, корректность бизнес-правил и миграций.
5. Изменение попадает в `main`.
6. Развёртывание выполняется по короткому release checklist.

Обязательный review нужен для:

- auth и authorization;
- платежей и скидок;
- provisioning Marzban;
- migrations;
- Docker/network/security configuration;
- backup/restore.

## 23. Этапы реализации

### Этап 0. Фиксация внешних решений

- Telegram Stars, валюта XTR, pre-checkout, successful payment и refund flow;
- домены и reverse proxy;
- Marzban version и OpenAPI;
- VLESS/TLS/REALITY конфигурация;
- валюта, возвраты и юридические требования;
- политика хранения данных и backup.

**Статус: завершён.** Архитектурные TBD закрыты решениями из `contracts/stage-0.decisions.json`; факты, требующие владельца домена, production bot/VPS или специалиста, оформлены как fail-closed evidence gates. Добавлены Telegram Stars fixtures, точный Marzban OpenAPI snapshot, контрактные тесты, backup runbook и CI без CD/staging.

### Этап 1. Основа приложения

- SvelteKit/Svelte 5/TypeScript/Tailwind;
- Docker development environment;
- Drizzle schema и migrations;
- Telegram auth и sessions;
- три секции и навигационный островок;
- theme integration.

### Этап 2. Каталог и админка

- тарифы;
- промокоды;
- FAQ;
- admin authorization и audit log;
- support tickets и Telegram notification.

### Этап 3. Покупка и VPN

- payment adapter;
- orders/payments/webhook;
- Marzban adapter;
- provisioning/reconciliation;
- профиль, история, активная подписка, QR.

### Этап 4. Hardening и запуск

- security review;
- rate limits и headers;
- E2E;
- backup/restore drill;
- мониторинг и alerting;
- smoke-test Telegram clients;
- release checklist и rollback.

## 24. Критерии приёмки

### 24.1. Авторизация и доступ

- Валидный пользователь Telegram входит без отдельной формы.
- Невалидный или устаревший `initData` отклоняется.
- Данные одного пользователя недоступны другому.
- Admin UI и endpoints недоступны пользователю с другим Telegram user ID.
- Изменение client-side user ID не даёт admin-доступ.

### 24.2. Интерфейс

- По умолчанию открывается «Главная».
- Поддержка расположена слева, профиль справа.
- Кнопки островка и свайп переключают секции.
- Активная секция очевидна.
- Навигация работает с клавиатуры и reduced motion.
- Glass-эффект имеет читаемый fallback.

### 24.3. Тарифы и промокоды

- Начальные тарифы 7/30/90 дней создаются seed-командой или через admin UI.
- Администратор может создать, изменить и деактивировать тариф.
- Исторический заказ не меняется после редактирования тарифа.
- Сервер игнорирует подменённую клиентом цену.
- Истёкший, выключенный или исчерпанный промокод не применяется.
- Лимит промокода корректен при конкурентных запросах.

### 24.4. Платёж и подписка

- Доступ не выдаётся по одному клиентскому redirect.
- Только проверенный `message.successful_payment` с полной сверкой запускает provisioning.
- Повтор Update, charge ID или `getStarTransactions` reconciliation не добавляет срок повторно.
- После успешного provisioning видны корректные QR и Subscription URL.
- При недоступности Marzban платёж сохраняется, заказ получает retryable status.
- После восстановления Marzban заказ можно завершить без повторной оплаты.

### 24.5. Поддержка

- FAQ отображаются в заданном порядке.
- Валидное обращение сохраняется и отправляется администратору.
- Ошибка Telegram Bot API не приводит к потере обращения.
- Пользовательский текст не ломает формат сообщения.

### 24.6. Эксплуатация

- Приложение поднимается по документированной Docker-команде.
- Внешне доступны только необходимые порты.
- Healthchecks отражают состояние сервисов.
- Backup создаётся и проверено восстановление.
- Логи не содержат секреты или полный Subscription URL.

## 25. Definition of Done

Функция считается завершённой, если:

- выполнены критерии приёмки;
- код соответствует существующей архитектуре и стилю;
- добавлены необходимые tests;
- format, lint, type check, tests и production build проходят;
- migration проверена на чистой базе и на копии предыдущей схемы, если она менялась;
- ошибки и loading/empty states обработаны;
- безопасность и журналирование проверены;
- пользовательская и эксплуатационная документация обновлена;
- изменение прошло ревью тимлида для критичных областей.

## 26. Production gates

Архитектурных TBD этапа 0 нет. Production launch блокируется конкретными проверяемыми evidence gates:

1. **Base domain/DNS:** передан принадлежащий проекту домен, настроены три host role и проверен HTTPS.
2. **Telegram webhook:** `setWebhook.secret_token` установлен и заголовок проверен негативным тестом.
3. **Stars live smoke:** выполнены контролируемые invoice, pre-checkout, successful payment и полный refund без отдельного staging.
4. **Bot owner security:** для Telegram-аккаунта владельца включена двухэтапная аутентификация.
5. **Terms/support:** работают `/terms` и `/paysupport`, согласие с условиями требуется до invoice.
6. **Merchant/legal status:** выбран и проверен статус самозанятого, ИП или организации.
7. **Tax/accounting review:** подтверждён учёт поступлений и вывода Telegram Stars, включая необходимость чеков.
8. **Legal review:** утверждены оферта, privacy/refund policy, законность VPN и schedule хранения платёжных документов.
9. **REALITY target:** target измерен с production VPS, подтверждены TLS 1.3 и SAN.
10. **REALITY secrets:** X25519 keys и short ID находятся в secret storage, проверены rotation/recovery.
11. **Marzban security:** зафиксированы SBOM/CVE scan Marzban image и bundled Xray.
12. **Offsite backup:** подтверждена зашифрованная копия у другого provider/account/region.
13. **Restore drill:** восстановление прошло в чистом окружении в пределах RPO/RTO.
14. **Telegram smoke:** проверены актуальные Telegram iOS, Android и Desktop; требуемая WebApp API version не ниже 6.1.
15. **Team lead review:** проверены payment, Marzban, security, backup и legal evidence.

До закрытия всех gates `productionReady=false`, реальные платежи и выдача реального доступа выключены.

Не блокируют начало разработки:

- финальные тексты и иллюстрации;
- аналитика продукта;
- реферальная программа;
- расширенные роли администраторов;
- мультирегиональная инфраструктура.

## 27. Источники по внешним контрактам

- Telegram Mini Apps и проверка `initData`: <https://core.telegram.org/bots/webapps>
- Документация Svelte: runes и Svelte 5: <https://svelte.dev/docs/svelte/what-are-runes>
- SvelteKit form actions: <https://svelte.dev/docs/kit/form-actions>
- SvelteKit authentication: <https://svelte.dev/docs/kit/auth>
- SvelteKit private environment variables: <https://svelte.dev/docs/kit/$env-static-private>
- Marzban: введение и возможности REST API: <https://gozargah.github.io/marzban/en/docs/introduction>
- Marzban v0.8.4: <https://github.com/Gozargah/Marzban/releases/tag/v0.8.4>
- Marzban API docs: <https://gozargah.github.io/marzban/en/docs/api>
- Marzban core/VLESS settings: <https://gozargah.github.io/marzban/en/docs/core-settings>
- Telegram Stars для цифровых товаров и услуг: <https://core.telegram.org/bots/payments-stars>
- Telegram Bot API 10.2: <https://core.telegram.org/bots/api>
- Telegram Mini App `openInvoice`: <https://core.telegram.org/bots/webapps>
- Telegram Stars refunds: <https://core.telegram.org/bots/api#refundstarpayment>
- Xray REALITY: <https://xtls.github.io/en/config/transports/reality.html>
- SQLite Online Backup API: <https://www.sqlite.org/backup.html>
- SQLite WAL: <https://www.sqlite.org/wal.html>
- restic encryption: <https://restic.readthedocs.io/en/stable/070_encryption.html>

Перед обновлением внешних адаптеров ссылки необходимо сверить с зафиксированными версиями Telegram Bot API, SvelteKit и Marzban.
