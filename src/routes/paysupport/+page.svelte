<script lang="ts">
  import { resolve } from "$app/paths";
</script>

<svelte:head>
  <title>Поддержка по оплате · VPN</title>
  <meta name="description" content="Помощь по оплате и возвратам" />
</svelte:head>

<main class="legal-page">
  <div class="legal-container">
    <a class="legal-back" href={resolve("/")}>← Назад</a>
    <h1 class="lg-h1 mt-3">Поддержка по оплате</h1>

    <p class="legal-notice">
      Предварительная редакция. Порядок возвратов уточняется вместе с
      юридической проверкой.
    </p>

    <section class="surface legal-block">
      <h2>Оплата прошла, доступа нет</h2>
      <p>
        Откройте приложение и проверьте профиль: если платёж подтверждён, но
        доступ ещё создаётся, отображается статус «Оплата получена». Обычно
        выдача занимает меньше минуты. Повторно оплачивать не нужно — заказ
        завершится автоматически.
      </p>

      <h2>Возврат</h2>
      <p>
        Возврат выполняется полностью и только по неиспользованному доступу.
        Частичный возврат не поддерживается. Для запроса укажите дату покупки и
        выбранный тариф в обращении.
      </p>

      <h2>Как обратиться</h2>
      <p>
        Раздел «Поддержка» в приложении — обращение попадает напрямую
        администратору, в ответ приходит номер обращения. В боте доступны
        команды <code>/terms</code> и <code>/paysupport</code>.
      </p>

      <h2>Сроки ответа</h2>
      <p>
        Обращения по оплате обрабатываются в приоритетном порядке. Ответ
        приходит в том же чате Telegram.
      </p>
    </section>

    <a class="lg-btn-glass legal-action" href={resolve("/terms")}>
      Условия использования
    </a>
  </div>
</main>
