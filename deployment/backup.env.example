# Copy to deployment/backup.env before enabling COMPOSE_PROFILES=backup.
# The restic password file is generated into DEPLOY_SECRET_DIRECTORY; copy it
# off the VPS before the first snapshot, otherwise restores are impossible.
BACKUP_INTERVAL_SECONDS=3600
RESTIC_REPOSITORY=
