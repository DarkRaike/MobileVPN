# Reference only. deployment/bootstrap/bootstrap.py writes the real worker.env
# into DEPLOY_SECRET_DIRECTORY on every stack start.
ENABLE_LIVE_OPERATIONS=false
INTERNAL_JOB_SECRET=
RECONCILIATION_INTERVAL_MILLISECONDS=30000
